services:
  portfolio:
    build:
      context: .
      target: build
    command: npm run dev -- --host 0.0.0.0
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - node_modules:/app/node_modules

  preview:
    build: .
    ports:
      - "8080:80"

volumes:
  node_modules:
